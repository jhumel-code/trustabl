Scan summary
  Repo:           C:\Users\Joana\Desktop\sairen-files\github\openai-agents-python
  Tools found:    210
  Findings:       407
  Overall score:    0%
  Risk score:     66.5/100

Per-tool readiness
  TestAgent                          0%  59.5/100  (7 findings)
  approval_tool                      0%  28.0/100  (29 findings)
  get_weather                        0%  28.0/100  (32 findings)
  test                               0%  66.5/100  (13 findings)
  test_agent                         0%  66.5/100  (6 findings)
  Assistant                          1%  59.5/100  (5 findings)
  assistant                         11%  66.5/100  (4 findings)
  sandbox                           11%  66.5/100  (4 findings)
  test-agent                        21%  59.5/100  (4 findings)
  dangerous_tool                    44%  28.0/100  (8 findings)
  write_file                        48%  49.0/100  (6 findings)
  read_file                         57%  49.0/100  (5 findings)
  approve_me                        58%  28.0/100  (6 findings)
  foo                               58%  28.0/100  (6 findings)
  needs_ok                          58%  28.0/100  (6 findings)
  reject_me                         58%  28.0/100  (6 findings)
  slow_tool                         58%  28.0/100  (6 findings)
                                    61%  38.0/100  (17 findings)
  faq_lookup_tool                   62%  28.0/100  (5 findings)
  update_seat                       67%  28.0/100  (4 findings)
  add_tool                          72%  28.0/100  (4 findings)
  alpha_tool                        72%  28.0/100  (4 findings)
  already_ran                       72%  28.0/100  (4 findings)
  bad_tool                          72%  28.0/100  (4 findings)
  beta_tool                         72%  28.0/100  (4 findings)
  deferred_lookup                   72%  28.0/100  (4 findings)
  fast_tool                         72%  28.0/100  (4 findings)
  inner_hitl_tool                   72%  28.0/100  (4 findings)
  lookup_account                    72%  28.0/100  (4 findings)
  pending_me                        72%  28.0/100  (4 findings)
  tool2                             72%  28.0/100  (4 findings)
  all_optional_params_function      73%  38.0/100  (3 findings)
  optional_param_function           73%  38.0/100  (3 findings)
  get_current_weather               74%  49.0/100  (3 findings)
  approval_echo                     77%  28.0/100  (3 findings)
  approval_note                     77%  28.0/100  (3 findings)
  get_discount_approval_rule        77%  28.0/100  (3 findings)
  f"Scenario {step.label}           78%  66.5/100  (1 findings)
  f"{label} HITL scenario           78%  66.5/100  (1 findings)
  first                             78%  66.5/100  (1 findings)
  DeepWiki Assistant                80%  59.5/100  (1 findings)
  FastAPI Agent                     80%  59.5/100  (1 findings)
  MCP Assistant                     80%  59.5/100  (1 findings)
  MCPAgent                          80%  59.5/100  (1 findings)
  SSE Assistant                     80%  59.5/100  (1 findings)
  ToolFetcher                       80%  59.5/100  (1 findings)
  _container_running                80%  59.5/100  (1 findings)
  _ensure_container                 80%  59.5/100  (1 findings)
  _run_python                       80%  59.5/100  (1 findings)
  _start_mcp_server                 80%  59.5/100  (1 findings)
  admin_user                        80%  59.5/100  (1 findings)
  build_docker_environment          80%  59.5/100  (1 findings)
  clone_repo                        80%  59.5/100  (1 findings)
  git_last_commit_timestamp         80%  59.5/100  (1 findings)
  interactive_shell_path            80%  59.5/100  (1 findings)
  local_sse_server                  80%  59.5/100  (1 findings)
  mcp-agent                         80%  59.5/100  (1 findings)
  open_file                         80%  59.5/100  (1 findings)
  prompt_agent                      80%  59.5/100  (1 findings)
  readonly_viewer                   80%  59.5/100  (1 findings)
  regular_user                      80%  59.5/100  (1 findings)
  run_examples                      80%  59.5/100  (1 findings)
  run_single                        80%  59.5/100  (1 findings)
  start_temporary_redis_server      80%  59.5/100  (1 findings)
  stream-mcp-agent                  80%  59.5/100  (1 findings)
  streamer                          80%  59.5/100  (1 findings)
  streaming_prompt_agent            80%  59.5/100  (1 findings)
  test_resolve_workspace_path_helper_allows_extra_root_symlink_target  80%  59.5/100  (1 findings)
  test_resolve_workspace_path_helper_allows_nested_read_only_extra_grant_on_read  80%  59.5/100  (1 findings)
  test_resolve_workspace_path_helper_rejects_extra_root_symlink_to_root  80%  59.5/100  (1 findings)
  test_resolve_workspace_path_helper_rejects_extra_root_when_not_allowed  80%  59.5/100  (1 findings)
  test_resolve_workspace_path_helper_rejects_nested_read_only_extra_grant_on_write  80%  59.5/100  (1 findings)
  test_tracing_atexit_cleanup_timeout_preserves_process_exit_code_on_504  80%  59.5/100  (1 findings)
  get_current_time                  81%  28.0/100  (2 findings)
  get_temperature                   81%  28.0/100  (2 findings)
  multiply_by_two                   81%  28.0/100  (2 findings)
  random_number                     81%  28.0/100  (2 findings)
  random_number_tool                81%  28.0/100  (2 findings)
  create_config                     83%  28.0/100  (2 findings)
  send_email                        83%  28.0/100  (2 findings)
  update_customer_record            83%  28.0/100  (2 findings)
  get_secret_word                   86%  14.3/100  (3 findings)
  another_tool                      86%  28.0/100  (2 findings)
  async_no_context                  86%  28.0/100  (2 findings)
  async_with_context                86%  28.0/100  (2 findings)
  deferred_lookup_account           86%  28.0/100  (2 findings)
  delegate_tool                     86%  28.0/100  (2 findings)
  disabled_tool                     86%  28.0/100  (2 findings)
  echo_tool                         86%  28.0/100  (2 findings)
  first_approval_tool               86%  28.0/100  (2 findings)
  foo_tool                          86%  28.0/100  (2 findings)
  get_current_timestamp             86%  28.0/100  (2 findings)
  get_user_name                     86%  28.0/100  (2 findings)
  greet                             86%  28.0/100  (2 findings)
  guarded                           86%  28.0/100  (2 findings)
  inner_sensitive_tool              86%  28.0/100  (2 findings)
  inner_shared_tool                 86%  28.0/100  (2 findings)
  inner_tool                        86%  28.0/100  (2 findings)
  lookup_customer_profile           86%  28.0/100  (2 findings)
  lookup_secret                     86%  28.0/100  (2 findings)
  other_tool                        86%  28.0/100  (2 findings)
  outer_shared_tool                 86%  28.0/100  (2 findings)
  outer_tool                        86%  28.0/100  (2 findings)
  pending_tool                      86%  28.0/100  (2 findings)
  record_side_effect                86%  28.0/100  (2 findings)
  second_approval_tool              86%  28.0/100  (2 findings)
  sync_no_context_no_args           86%  28.0/100  (2 findings)
  sync_no_context_override          86%  28.0/100  (2 findings)
  sync_no_context_with_args         86%  28.0/100  (2 findings)
  sync_tool_decorator_style         86%  28.0/100  (2 findings)
  sync_with_context                 86%  28.0/100  (2 findings)
  synthetic_tool                    86%  28.0/100  (2 findings)
  test_tool_one                     86%  28.0/100  (2 findings)
  test_tool_two                     86%  28.0/100  (2 findings)
  timeout_configured_tool           86%  28.0/100  (2 findings)
  tool_one                          86%  28.0/100  (2 findings)
  triage_tool                       86%  28.0/100  (2 findings)
  visible_lookup_account            86%  28.0/100  (2 findings)
  timeout_tool                      87%  24.0/100  (2 findings)
  billing_status_checker            91%  28.0/100  (1 findings)
  calculate_sum                     91%  28.0/100  (1 findings)
  fetch_random_image                91%  28.0/100  (1 findings)
  format_message                    91%  28.0/100  (1 findings)
  get_contact_info                  91%  28.0/100  (1 findings)
  get_customer_profile              91%  28.0/100  (1 findings)
  get_discount_approval_path        91%  28.0/100  (1 findings)
  get_invoice_status                91%  28.0/100  (1 findings)
  get_shipping_credit_balance       91%  28.0/100  (1 findings)
  get_shipping_eta                  91%  28.0/100  (1 findings)
  get_user_data                     91%  28.0/100  (1 findings)
  helper                            91%  28.0/100  (1 findings)
  how_many_jokes                    91%  28.0/100  (1 findings)
  list_open_orders                  91%  28.0/100  (1 findings)
  lookup_insurance_eligibility      91%  28.0/100  (1 findings)
  lookup_order                      91%  28.0/100  (1 findings)
  lookup_patient                    91%  28.0/100  (1 findings)
  lookup_referral_status            91%  28.0/100  (1 findings)
  ping                              91%  28.0/100  (1 findings)
  publish_announcement              91%  28.0/100  (1 findings)
  query_runloop_network_policy      91%  28.0/100  (1 findings)
  query_runloop_secret              91%  28.0/100  (1 findings)
  route_to_human_queue              91%  28.0/100  (1 findings)
  submit_refund                     91%  28.0/100  (1 findings)
  test_tool                         91%  28.0/100  (1 findings)
  boom                              92%  24.0/100  (1 findings)
  add                               95%  14.3/100  (1 findings)
  apply_lifecycle_patch             95%  14.3/100  (1 findings)
  assert_manifest_materialized_tool  95%  14.3/100  (1 findings)
  assert_restored_lifecycle_state_tool  95%  14.3/100  (1 findings)
  assert_workspace_escape_blocked_tool  95%  14.3/100  (1 findings)
  echo                              95%  14.3/100  (1 findings)
  extract_lifecycle_archive         95%  14.3/100  (1 findings)
  start_lifecycle_pty               95%  14.3/100  (1 findings)
  will_fail_on_bad_json             95%  14.3/100  (1 findings)
  will_not_fail_on_bad_json         95%  14.3/100  (1 findings)
  will_not_fail_on_bad_json_async   95%  14.3/100  (1 findings)
  get_policy_reference             100%  0.0/100  (0 findings)


Config & permission findings  (no code change required)

  TestAgent
    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (tests/mcp/test_mcp_approval.py:23)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.

    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (tests/mcp/test_mcp_approval.py:55)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.

    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (tests/mcp/test_mcp_approval.py:80)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.

    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (tests/mcp/test_mcp_approval.py:106)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.

    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (tests/mcp/test_mcp_approval.py:171)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.

    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (tests/mcp/test_mcp_approval.py:216)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.

    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (tests/test_hitl_error_scenarios.py:1371)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.


  test
    [OAI-102] HIGH Agent uses tool_use_behavior="stop_on_first_tool"  (tests/test_agent_runner.py:3259)
        With tool_use_behavior="stop_on_first_tool", the first tool's raw output becomes the       
        agent's final response without any model post-processing. If the tool returns      
        attacker-controlled data (web search results, untrusted file contents, MCP-provided
        tool output), that data is rendered to the user verbatim — an exfiltration and     
        prompt-injection vector.                                                           
        fix: Remove the tool_use_behavior="stop_on_first_tool" kwarg (the default "run_llm_again" is safe) or constrain it via StopAtTools(...) listing only tools whose outputs you control.

    [OAI-102] HIGH Agent uses tool_use_behavior="stop_on_first_tool"  (tests/test_tool_use_behavior.py:77)
        With tool_use_behavior="stop_on_first_tool", the first tool's raw output becomes the       
        agent's final response without any model post-processing. If the tool returns      
        attacker-controlled data (web search results, untrusted file contents, MCP-provided
        tool output), that data is rendered to the user verbatim — an exfiltration and     
        prompt-injection vector.                                                           
        fix: Remove the tool_use_behavior="stop_on_first_tool" kwarg (the default "run_llm_again" is safe) or constrain it via StopAtTools(...) listing only tools whose outputs you control.

    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (tests/mcp/test_mcp_tracing.py:17)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.

    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (tests/mcp/test_mcp_tracing.py:224)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.

    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (tests/mcp/test_runner_calls_mcp.py:33)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.

    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (tests/mcp/test_runner_calls_mcp.py:67)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.

    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (tests/mcp/test_runner_calls_mcp.py:103)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.

    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (tests/mcp/test_runner_calls_mcp.py:142)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.

    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (tests/mcp/test_runner_calls_mcp.py:176)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.

    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (tests/mcp/test_runner_calls_mcp.py:224)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.

    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (tests/mcp/test_runner_calls_mcp.py:271)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.

    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (tests/mcp/test_runner_calls_mcp.py:318)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.

    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (tests/mcp/test_runner_calls_mcp.py:366)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.


  test_agent
    [OAI-103] HIGH tool_choice="required" combined with reset_tool_choice=False  (tests/test_tool_choice_reset.py:207)
        The agent forces tool use on every turn (tool_choice="required") AND does not reset     
        the tool choice after each call (reset_tool_choice=False). This is the SDK's    
        documented loop pattern: the model is required to call a tool, calls one, and is
        required to call another, indefinitely.                                         
        fix: Either drop reset_tool_choice=False (the default True breaks the loop) or change tool_choice to "auto". If forced tool use is intentional, ensure at least one of the tools terminates the agent via tool_use_behavior=StopAtTools(...).

    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (tests/mcp/test_mcp_util.py:399)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.

    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (tests/mcp/test_mcp_util.py:447)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.

    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (tests/mcp/test_mcp_util.py:1370)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.

    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (tests/mcp/test_mcp_util.py:1435)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.

    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (tests/mcp/test_mcp_util.py:1535)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.


  Assistant
    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (examples/mcp/filesystem_example/main.py:10)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.

    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (examples/mcp/git_example/main.py:10)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.

    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (examples/mcp/sse_example/main.py:32)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.

    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (examples/mcp/streamablehttp_custom_client_example/main.py:73)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.

    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (examples/mcp/streamablehttp_example/main.py:32)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.


  assistant
    [OAI-102] HIGH Agent uses tool_use_behavior="stop_on_first_tool"  (tests/memory/test_openai_responses_compaction_session.py:1069)
        With tool_use_behavior="stop_on_first_tool", the first tool's raw output becomes the       
        agent's final response without any model post-processing. If the tool returns      
        attacker-controlled data (web search results, untrusted file contents, MCP-provided
        tool output), that data is rendered to the user verbatim — an exfiltration and     
        prompt-injection vector.                                                           
        fix: Remove the tool_use_behavior="stop_on_first_tool" kwarg (the default "run_llm_again" is safe) or constrain it via StopAtTools(...) listing only tools whose outputs you control.

    [OAI-102] HIGH Agent uses tool_use_behavior="stop_on_first_tool"  (tests/memory/test_openai_responses_compaction_session.py:1101)
        With tool_use_behavior="stop_on_first_tool", the first tool's raw output becomes the       
        agent's final response without any model post-processing. If the tool returns      
        attacker-controlled data (web search results, untrusted file contents, MCP-provided
        tool output), that data is rendered to the user verbatim — an exfiltration and     
        prompt-injection vector.                                                           
        fix: Remove the tool_use_behavior="stop_on_first_tool" kwarg (the default "run_llm_again" is safe) or constrain it via StopAtTools(...) listing only tools whose outputs you control.

    [OAI-102] HIGH Agent uses tool_use_behavior="stop_on_first_tool"  (tests/memory/test_openai_responses_compaction_session.py:1143)
        With tool_use_behavior="stop_on_first_tool", the first tool's raw output becomes the       
        agent's final response without any model post-processing. If the tool returns      
        attacker-controlled data (web search results, untrusted file contents, MCP-provided
        tool output), that data is rendered to the user verbatim — an exfiltration and     
        prompt-injection vector.                                                           
        fix: Remove the tool_use_behavior="stop_on_first_tool" kwarg (the default "run_llm_again" is safe) or constrain it via StopAtTools(...) listing only tools whose outputs you control.

    [OAI-102] HIGH Agent uses tool_use_behavior="stop_on_first_tool"  (tests/memory/test_openai_responses_compaction_session.py:1186)
        With tool_use_behavior="stop_on_first_tool", the first tool's raw output becomes the       
        agent's final response without any model post-processing. If the tool returns      
        attacker-controlled data (web search results, untrusted file contents, MCP-provided
        tool output), that data is rendered to the user verbatim — an exfiltration and     
        prompt-injection vector.                                                           
        fix: Remove the tool_use_behavior="stop_on_first_tool" kwarg (the default "run_llm_again" is safe) or constrain it via StopAtTools(...) listing only tools whose outputs you control.


  sandbox
    [OAI-102] HIGH Agent uses tool_use_behavior="stop_on_first_tool"  (tests/test_run_state.py:462)
        With tool_use_behavior="stop_on_first_tool", the first tool's raw output becomes the       
        agent's final response without any model post-processing. If the tool returns      
        attacker-controlled data (web search results, untrusted file contents, MCP-provided
        tool output), that data is rendered to the user verbatim — an exfiltration and     
        prompt-injection vector.                                                           
        fix: Remove the tool_use_behavior="stop_on_first_tool" kwarg (the default "run_llm_again" is safe) or constrain it via StopAtTools(...) listing only tools whose outputs you control.

    [OAI-102] HIGH Agent uses tool_use_behavior="stop_on_first_tool"  (tests/test_run_state.py:476)
        With tool_use_behavior="stop_on_first_tool", the first tool's raw output becomes the       
        agent's final response without any model post-processing. If the tool returns      
        attacker-controlled data (web search results, untrusted file contents, MCP-provided
        tool output), that data is rendered to the user verbatim — an exfiltration and     
        prompt-injection vector.                                                           
        fix: Remove the tool_use_behavior="stop_on_first_tool" kwarg (the default "run_llm_again" is safe) or constrain it via StopAtTools(...) listing only tools whose outputs you control.

    [OAI-102] HIGH Agent uses tool_use_behavior="stop_on_first_tool"  (tests/test_run_state.py:528)
        With tool_use_behavior="stop_on_first_tool", the first tool's raw output becomes the       
        agent's final response without any model post-processing. If the tool returns      
        attacker-controlled data (web search results, untrusted file contents, MCP-provided
        tool output), that data is rendered to the user verbatim — an exfiltration and     
        prompt-injection vector.                                                           
        fix: Remove the tool_use_behavior="stop_on_first_tool" kwarg (the default "run_llm_again" is safe) or constrain it via StopAtTools(...) listing only tools whose outputs you control.

    [OAI-102] HIGH Agent uses tool_use_behavior="stop_on_first_tool"  (tests/test_run_state.py:546)
        With tool_use_behavior="stop_on_first_tool", the first tool's raw output becomes the       
        agent's final response without any model post-processing. If the tool returns      
        attacker-controlled data (web search results, untrusted file contents, MCP-provided
        tool output), that data is rendered to the user verbatim — an exfiltration and     
        prompt-injection vector.                                                           
        fix: Remove the tool_use_behavior="stop_on_first_tool" kwarg (the default "run_llm_again" is safe) or constrain it via StopAtTools(...) listing only tools whose outputs you control.


  test-agent
    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (tests/mcp/test_mcp_util.py:1241)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.

    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (tests/mcp/test_mcp_util.py:1275)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.

    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (tests/mcp/test_mcp_util.py:1303)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.

    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (tests/mcp/test_mcp_util.py:1329)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.


  
    [OSH-004]  MED No OpenShell resource limits configured  (:0)
        The repo declares no OpenShell policy with cpu/memory/time limits. A runaway tool call
        (infinite loop, memory blow-up) is unbounded by default.                      
        fix: The generated openshell/policy.yaml will include default limits — review them against your expected workloads before committing.


  get_current_weather
    [OSH-005] HIGH Network egress is unrestricted  (examples/mcp/streamablehttp_example/server.py:28)
        The tool issues HTTP calls to a URL derived from inputs, with no host allowlist in the       
        code path. OpenShell can restrict outbound DNS/connect at the sandbox layer, which is
        strictly more reliable than enforcing it in Python.                                  
        fix: Define an allowlist of hostnames the tool may reach. The generated OpenShell policy will gate egress to those hosts.


  f"Scenario {step.label}
    [OAI-102] HIGH Agent uses tool_use_behavior="stop_on_first_tool"  (tests/test_hitl_session_scenario.py:286)
        With tool_use_behavior="stop_on_first_tool", the first tool's raw output becomes the       
        agent's final response without any model post-processing. If the tool returns      
        attacker-controlled data (web search results, untrusted file contents, MCP-provided
        tool output), that data is rendered to the user verbatim — an exfiltration and     
        prompt-injection vector.                                                           
        fix: Remove the tool_use_behavior="stop_on_first_tool" kwarg (the default "run_llm_again" is safe) or constrain it via StopAtTools(...) listing only tools whose outputs you control.


  f"{label} HITL scenario
    [OAI-102] HIGH Agent uses tool_use_behavior="stop_on_first_tool"  (examples/memory/hitl_session_scenario.py:77)
        With tool_use_behavior="stop_on_first_tool", the first tool's raw output becomes the       
        agent's final response without any model post-processing. If the tool returns      
        attacker-controlled data (web search results, untrusted file contents, MCP-provided
        tool output), that data is rendered to the user verbatim — an exfiltration and     
        prompt-injection vector.                                                           
        fix: Remove the tool_use_behavior="stop_on_first_tool" kwarg (the default "run_llm_again" is safe) or constrain it via StopAtTools(...) listing only tools whose outputs you control.


  first
    [OAI-102] HIGH Agent uses tool_use_behavior="stop_on_first_tool"  (tests/test_example_workflows.py:745)
        With tool_use_behavior="stop_on_first_tool", the first tool's raw output becomes the       
        agent's final response without any model post-processing. If the tool returns      
        attacker-controlled data (web search results, untrusted file contents, MCP-provided
        tool output), that data is rendered to the user verbatim — an exfiltration and     
        prompt-injection vector.                                                           
        fix: Remove the tool_use_behavior="stop_on_first_tool" kwarg (the default "run_llm_again" is safe) or constrain it via StopAtTools(...) listing only tools whose outputs you control.


  DeepWiki Assistant
    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (examples/mcp/streamable_http_remote_example/main.py:21)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.


  FastAPI Agent
    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (examples/mcp/manager_example/app.py:101)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.


  MCP Assistant
    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (examples/mcp/tool_filter_example/main.py:42)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.


  MCPAgent
    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (tests/test_stream_events.py:139)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.


  SSE Assistant
    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (examples/mcp/sse_remote_example/main.py:71)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.


  ToolFetcher
    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (examples/mcp/get_all_mcp_tools_example/main.py:17)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.


  _container_running
    [OSH-002] HIGH Shell invocation without an allowed-command list  (examples/memory/dapr_session_example.py:445)
        The tool shells out but does not constrain which binaries can be invoked. Even with        
        `shell=False`, an unconstrained `argv[0]` lets the model invoke arbitrary installed
        binaries. OpenShell can enforce an allowlist at the sandbox layer.                 
        fix: Define a constant ALLOWED_COMMANDS and assert argv[0] is in it. The generated OpenShell policy will also include a command allowlist.


  _ensure_container
    [OSH-002] HIGH Shell invocation without an allowed-command list  (examples/memory/dapr_session_example.py:462)
        The tool shells out but does not constrain which binaries can be invoked. Even with        
        `shell=False`, an unconstrained `argv[0]` lets the model invoke arbitrary installed
        binaries. OpenShell can enforce an allowlist at the sandbox layer.                 
        fix: Define a constant ALLOWED_COMMANDS and assert argv[0] is in it. The generated OpenShell policy will also include a command allowlist.


  _run_python
    [OSH-002] HIGH Shell invocation without an allowed-command list  (tests/tracing/test_import_side_effects.py:14)
        The tool shells out but does not constrain which binaries can be invoked. Even with        
        `shell=False`, an unconstrained `argv[0]` lets the model invoke arbitrary installed
        binaries. OpenShell can enforce an allowlist at the sandbox layer.                 
        fix: Define a constant ALLOWED_COMMANDS and assert argv[0] is in it. The generated OpenShell policy will also include a command allowlist.


  _start_mcp_server
    [OSH-002] HIGH Shell invocation without an allowed-command list  (examples/mcp/manager_example/smoke_test.py:59)
        The tool shells out but does not constrain which binaries can be invoked. Even with        
        `shell=False`, an unconstrained `argv[0]` lets the model invoke arbitrary installed
        binaries. OpenShell can enforce an allowlist at the sandbox layer.                 
        fix: Define a constant ALLOWED_COMMANDS and assert argv[0] is in it. The generated OpenShell policy will also include a command allowlist.


  admin_user
    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (tests/mcp/test_tool_filtering.py:222)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.


  build_docker_environment
    [OSH-002] HIGH Shell invocation without an allowed-command list  (examples/sandbox/tutorials/misc.py:135)
        The tool shells out but does not constrain which binaries can be invoked. Even with        
        `shell=False`, an unconstrained `argv[0]` lets the model invoke arbitrary installed
        binaries. OpenShell can enforce an allowlist at the sandbox layer.                 
        fix: Define a constant ALLOWED_COMMANDS and assert argv[0] is in it. The generated OpenShell policy will also include a command allowlist.


  clone_repo
    [OSH-002] HIGH Shell invocation without an allowed-command list  (src/agents/sandbox/util/github.py:13)
        The tool shells out but does not constrain which binaries can be invoked. Even with        
        `shell=False`, an unconstrained `argv[0]` lets the model invoke arbitrary installed
        binaries. OpenShell can enforce an allowlist at the sandbox layer.                 
        fix: Define a constant ALLOWED_COMMANDS and assert argv[0] is in it. The generated OpenShell policy will also include a command allowlist.


  git_last_commit_timestamp
    [OSH-002] HIGH Shell invocation without an allowed-command list  (docs/scripts/translate_docs.py:374)
        The tool shells out but does not constrain which binaries can be invoked. Even with        
        `shell=False`, an unconstrained `argv[0]` lets the model invoke arbitrary installed
        binaries. OpenShell can enforce an allowlist at the sandbox layer.                 
        fix: Define a constant ALLOWED_COMMANDS and assert argv[0] is in it. The generated OpenShell policy will also include a command allowlist.


  interactive_shell_path
    [OSH-002] HIGH Shell invocation without an allowed-command list  (examples/run_examples.py:181)
        The tool shells out but does not constrain which binaries can be invoked. Even with        
        `shell=False`, an unconstrained `argv[0]` lets the model invoke arbitrary installed
        binaries. OpenShell can enforce an allowlist at the sandbox layer.                 
        fix: Define a constant ALLOWED_COMMANDS and assert argv[0] is in it. The generated OpenShell policy will also include a command allowlist.


  local_sse_server
    [OSH-002] HIGH Shell invocation without an allowed-command list  (examples/mcp/sse_remote_example/main.py:31)
        The tool shells out but does not constrain which binaries can be invoked. Even with        
        `shell=False`, an unconstrained `argv[0]` lets the model invoke arbitrary installed
        binaries. OpenShell can enforce an allowlist at the sandbox layer.                 
        fix: Define a constant ALLOWED_COMMANDS and assert argv[0] is in it. The generated OpenShell policy will also include a command allowlist.


  mcp-agent
    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (tests/test_tool_origin.py:153)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.


  open_file
    [OSH-002] HIGH Shell invocation without an allowed-command list  (examples/tools/image_generator.py:20)
        The tool shells out but does not constrain which binaries can be invoked. Even with        
        `shell=False`, an unconstrained `argv[0]` lets the model invoke arbitrary installed
        binaries. OpenShell can enforce an allowlist at the sandbox layer.                 
        fix: Define a constant ALLOWED_COMMANDS and assert argv[0] is in it. The generated OpenShell policy will also include a command allowlist.


  prompt_agent
    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (tests/mcp/test_prompt_server.py:176)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.


  readonly_viewer
    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (tests/mcp/test_tool_filtering.py:228)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.


  regular_user
    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (tests/mcp/test_tool_filtering.py:234)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.


  run_examples
    [OSH-002] HIGH Shell invocation without an allowed-command list  (examples/run_examples.py:611)
        The tool shells out but does not constrain which binaries can be invoked. Even with        
        `shell=False`, an unconstrained `argv[0]` lets the model invoke arbitrary installed
        binaries. OpenShell can enforce an allowlist at the sandbox layer.                 
        fix: Define a constant ALLOWED_COMMANDS and assert argv[0] is in it. The generated OpenShell policy will also include a command allowlist.


  run_single
    [OSH-002] HIGH Shell invocation without an allowed-command list  (examples/run_examples.py:665)
        The tool shells out but does not constrain which binaries can be invoked. Even with        
        `shell=False`, an unconstrained `argv[0]` lets the model invoke arbitrary installed
        binaries. OpenShell can enforce an allowlist at the sandbox layer.                 
        fix: Define a constant ALLOWED_COMMANDS and assert argv[0] is in it. The generated OpenShell policy will also include a command allowlist.


  start_temporary_redis_server
    [OSH-002] HIGH Shell invocation without an allowed-command list  (examples/run_examples.py:303)
        The tool shells out but does not constrain which binaries can be invoked. Even with        
        `shell=False`, an unconstrained `argv[0]` lets the model invoke arbitrary installed
        binaries. OpenShell can enforce an allowlist at the sandbox layer.                 
        fix: Define a constant ALLOWED_COMMANDS and assert argv[0] is in it. The generated OpenShell policy will also include a command allowlist.


  stream-mcp-agent
    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (tests/test_tool_origin.py:182)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.


  streamer
    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (tests/test_agent_as_tool.py:2033)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.


  streaming_prompt_agent
    [OAI-105] HIGH Agent has mcp_servers configured AND no input_guardrails  (tests/mcp/test_prompt_server.py:214)
        The agent imports tools from one or more MCP servers (mcp_servers=) but has no              
        input_guardrails. MCP tool descriptions are advertised by the MCP server, which is a
        separate trust boundary; an attacker- controlled or compromised MCP server can craft
        tool descriptions that bait the agent into harmful actions (tool poisoning).        
        fix: Add at least one @input_guardrail that inspects the user's input AND the resolved tool list before the model is invoked. Pin MCP servers to known-trusted URLs/checksums and document the trust assumption.


  test_resolve_workspace_path_helper_allows_extra_root_symlink_target
    [OSH-002] HIGH Shell invocation without an allowed-command list  (tests/sandbox/test_runtime_helpers.py:39)
        The tool shells out but does not constrain which binaries can be invoked. Even with        
        `shell=False`, an unconstrained `argv[0]` lets the model invoke arbitrary installed
        binaries. OpenShell can enforce an allowlist at the sandbox layer.                 
        fix: Define a constant ALLOWED_COMMANDS and assert argv[0] is in it. The generated OpenShell policy will also include a command allowlist.


  test_resolve_workspace_path_helper_allows_nested_read_only_extra_grant_on_read
    [OSH-002] HIGH Shell invocation without an allowed-command list  (tests/sandbox/test_runtime_helpers.py:168)
        The tool shells out but does not constrain which binaries can be invoked. Even with        
        `shell=False`, an unconstrained `argv[0]` lets the model invoke arbitrary installed
        binaries. OpenShell can enforce an allowlist at the sandbox layer.                 
        fix: Define a constant ALLOWED_COMMANDS and assert argv[0] is in it. The generated OpenShell policy will also include a command allowlist.


  test_resolve_workspace_path_helper_rejects_extra_root_symlink_to_root
    [OSH-002] HIGH Shell invocation without an allowed-command list  (tests/sandbox/test_runtime_helpers.py:99)
        The tool shells out but does not constrain which binaries can be invoked. Even with        
        `shell=False`, an unconstrained `argv[0]` lets the model invoke arbitrary installed
        binaries. OpenShell can enforce an allowlist at the sandbox layer.                 
        fix: Define a constant ALLOWED_COMMANDS and assert argv[0] is in it. The generated OpenShell policy will also include a command allowlist.


  test_resolve_workspace_path_helper_rejects_extra_root_when_not_allowed
    [OSH-002] HIGH Shell invocation without an allowed-command list  (tests/sandbox/test_runtime_helpers.py:69)
        The tool shells out but does not constrain which binaries can be invoked. Even with        
        `shell=False`, an unconstrained `argv[0]` lets the model invoke arbitrary installed
        binaries. OpenShell can enforce an allowlist at the sandbox layer.                 
        fix: Define a constant ALLOWED_COMMANDS and assert argv[0] is in it. The generated OpenShell policy will also include a command allowlist.


  test_resolve_workspace_path_helper_rejects_nested_read_only_extra_grant_on_write
    [OSH-002] HIGH Shell invocation without an allowed-command list  (tests/sandbox/test_runtime_helpers.py:130)
        The tool shells out but does not constrain which binaries can be invoked. Even with        
        `shell=False`, an unconstrained `argv[0]` lets the model invoke arbitrary installed
        binaries. OpenShell can enforce an allowlist at the sandbox layer.                 
        fix: Define a constant ALLOWED_COMMANDS and assert argv[0] is in it. The generated OpenShell policy will also include a command allowlist.


  test_tracing_atexit_cleanup_timeout_preserves_process_exit_code_on_504
    [OSH-002] HIGH Shell invocation without an allowed-command list  (tests/test_trace_processor.py:559)
        The tool shells out but does not constrain which binaries can be invoked. Even with        
        `shell=False`, an unconstrained `argv[0]` lets the model invoke arbitrary installed
        binaries. OpenShell can enforce an allowlist at the sandbox layer.                 
        fix: Define a constant ALLOWED_COMMANDS and assert argv[0] is in it. The generated OpenShell policy will also include a command allowlist.


  [OSH-004]  MED No OpenShell resource limits configured  (:0)
      The repo declares no OpenShell policy with cpu/memory/time limits. A runaway tool call
        (infinite loop, memory blow-up) is unbounded by default.                      
      fix: The generated openshell/policy.yaml will include default limits — review them against your expected workloads before committing.


Code findings  (source change required)

  approval_tool
    [OAI-004]  MED Tool has no failure_error_function  (tests/sandbox/integration_tests/test_runner_pause_resume.py:75)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/sandbox/test_runtime.py:1839)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/sandbox/test_runtime.py:2413)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/sandbox/test_runtime.py:2513)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/sandbox/test_runtime.py:2619)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/sandbox/test_runtime.py:3568)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/sandbox/test_runtime.py:3664)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/sandbox/test_runtime.py:4738)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_agent_runner.py:800)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_agent_runner.py:1040)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_agent_runner_streamed.py:1742)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_agent_tracing.py:27)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_run_state.py:579)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_tool_origin.py:99)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/sandbox/integration_tests/test_runner_pause_resume.py:75)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/sandbox/test_runtime.py:1839)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/sandbox/test_runtime.py:2413)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/sandbox/test_runtime.py:2513)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/sandbox/test_runtime.py:2619)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/sandbox/test_runtime.py:3568)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/sandbox/test_runtime.py:3664)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/sandbox/test_runtime.py:4738)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_agent_runner.py:800)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_agent_runner.py:1040)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_agent_runner_streamed.py:1742)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_agent_tracing.py:27)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_run_state.py:579)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_tool_origin.py:99)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-005]  MED OpenAI tool raises exceptions without a structured error contract  (tests/test_tool_origin.py:99)
        When a @function_tool raises, the SDK surfaces the exception to the model as an opaque       
        string. The model often cannot recover or retry intelligently. Returning a structured
        error object lets the model branch on the failure mode and decide whether to retry or
        escalate.                                                                            
        fix: Catch known failure modes and return a structured payload such as `{"error": "...", "retryable": False}` instead of raising.


  get_weather
    [OAI-004]  MED Tool has no failure_error_function  (examples/agent_patterns/forcing_tool_use.py:47)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (examples/agent_patterns/human_in_the_loop.py:19)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (examples/agent_patterns/human_in_the_loop_stream.py:39)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (examples/basic/tools.py:16)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (examples/basic/usage_tracking.py:15)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (examples/memory/advanced_sqlite_session_example.py:16)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (examples/memory/memory_session_hitl_example.py:21)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (examples/memory/openai_session_hitl_example.py:21)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (examples/model_providers/any_llm_auto.py:18)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (examples/model_providers/any_llm_provider.py:21)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (examples/model_providers/custom_example_agent.py:36)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (examples/model_providers/custom_example_global.py:45)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (examples/model_providers/custom_example_provider.py:52)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (examples/model_providers/litellm_auto.py:21)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (examples/model_providers/litellm_provider.py:22)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (examples/realtime/app/agent.py:52)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (examples/realtime/cli/demo.py:38)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (examples/realtime/twilio/twilio_handler.py:24)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (examples/voice/static/main.py:34)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (examples/voice/streamed/my_workflow.py:10)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_example_workflows.py:709)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_function_tool_decorator.py:218)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (examples/agent_patterns/forcing_tool_use.py:47)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (examples/memory/advanced_sqlite_session_example.py:16)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (examples/model_providers/any_llm_auto.py:18)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (examples/model_providers/any_llm_provider.py:21)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (examples/model_providers/custom_example_agent.py:36)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (examples/model_providers/custom_example_global.py:45)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (examples/model_providers/custom_example_provider.py:52)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (examples/model_providers/litellm_auto.py:21)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (examples/model_providers/litellm_provider.py:22)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_example_workflows.py:709)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  dangerous_tool
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_guardrails.py:672)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_guardrails.py:730)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_guardrails.py:808)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_guardrails.py:849)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_guardrails.py:672)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_guardrails.py:730)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_guardrails.py:808)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_guardrails.py:849)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  write_file
    [OAI-004]  MED Tool has no failure_error_function  (examples/basic/stream_function_call_args.py:10)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/sandbox/test_runtime.py:802)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-006] HIGH Tool accepts path without normalization  (tests/sandbox/test_runtime.py:802)
        The tool accepts a path-like parameter and flows it into a filesystem call (open,           
        Path(...), shutil.*, os.*) without calling .resolve() or os.path.realpath() on it   
        first. The agent's input may contain .. segments that escape the intended directory,
        exposing arbitrary files on the host.                                               
        fix: Normalize the path before use: p = Path(file_path).resolve(). Then check that the resolved path is inside an explicit allowed-root directory before opening.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/sandbox/integration_tests/_helpers.py:346)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/sandbox/test_runtime.py:802)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-005]  MED OpenAI tool raises exceptions without a structured error contract  (tests/sandbox/integration_tests/_helpers.py:346)
        When a @function_tool raises, the SDK surfaces the exception to the model as an opaque       
        string. The model often cannot recover or retry intelligently. Returning a structured
        error object lets the model branch on the failure mode and decide whether to retry or
        escalate.                                                                            
        fix: Catch known failure modes and return a structured payload such as `{"error": "...", "retryable": False}` instead of raising.


  read_file
    [OAI-004]  MED Tool has no failure_error_function  (tests/sandbox/test_runtime.py:808)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-006] HIGH Tool accepts path without normalization  (tests/sandbox/test_runtime.py:808)
        The tool accepts a path-like parameter and flows it into a filesystem call (open,           
        Path(...), shutil.*, os.*) without calling .resolve() or os.path.realpath() on it   
        first. The agent's input may contain .. segments that escape the intended directory,
        exposing arbitrary files on the host.                                               
        fix: Normalize the path before use: p = Path(file_path).resolve(). Then check that the resolved path is inside an explicit allowed-root directory before opening.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/sandbox/integration_tests/_helpers.py:353)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/sandbox/test_runtime.py:808)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-005]  MED OpenAI tool raises exceptions without a structured error contract  (tests/sandbox/integration_tests/_helpers.py:353)
        When a @function_tool raises, the SDK surfaces the exception to the model as an opaque       
        string. The model often cannot recover or retry intelligently. Returning a structured
        error object lets the model branch on the failure mode and decide whether to retry or
        escalate.                                                                            
        fix: Catch known failure modes and return a structured payload such as `{"error": "...", "retryable": False}` instead of raising.


  approve_me
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_hitl_error_scenarios.py:997)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_hitl_error_scenarios.py:1259)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_hitl_error_scenarios.py:1313)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_hitl_error_scenarios.py:997)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_hitl_error_scenarios.py:1259)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_hitl_error_scenarios.py:1313)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  foo
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_agent_runner.py:852)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_stream_events.py:82)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_stream_events.py:280)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_agent_runner.py:852)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_stream_events.py:82)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_stream_events.py:280)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  needs_ok
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_run_impl_resume_paths.py:356)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_run_state.py:5901)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_run_state.py:5924)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_run_impl_resume_paths.py:356)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_run_state.py:5901)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_run_state.py:5924)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  reject_me
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_hitl_error_scenarios.py:1487)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_hitl_error_scenarios.py:1545)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_hitl_error_scenarios.py:2063)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_hitl_error_scenarios.py:1487)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_hitl_error_scenarios.py:1545)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_hitl_error_scenarios.py:2063)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  slow_tool
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_function_tool.py:834)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_function_tool.py:857)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_function_tool.py:874)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_function_tool.py:834)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_function_tool.py:857)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_function_tool.py:874)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  faq_lookup_tool
    [OAI-004]  MED Tool has no failure_error_function  (examples/customer_service/main.py:42)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (examples/realtime/app/agent.py:17)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (examples/realtime/twilio_sip/agents.py:20)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (examples/customer_service/main.py:42)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (examples/realtime/app/agent.py:17)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  update_seat
    [OAI-004]  MED Tool has no failure_error_function  (examples/customer_service/main.py:68)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (examples/realtime/app/agent.py:40)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-006]  MED Mutating OpenAI tool has no idempotency key  (examples/customer_service/main.py:68)
        Tool name suggests a side effect (create/send/refund/…). The OpenAI Agents SDK              
        retries tool calls on timeout and ambiguous failures; without an idempotency key the
        same action can fire twice. Downstream services must also honor the key for this    
        protection to be effective.                                                         
        fix: Add an `idempotency_key: str` parameter and pass it to the backing API.

    [OAIS-006]  MED Mutating OpenAI tool has no idempotency key  (examples/realtime/app/agent.py:40)
        Tool name suggests a side effect (create/send/refund/…). The OpenAI Agents SDK              
        retries tool calls on timeout and ambiguous failures; without an idempotency key the
        same action can fire twice. Downstream services must also honor the key for this    
        protection to be effective.                                                         
        fix: Add an `idempotency_key: str` parameter and pass it to the backing API.


  add_tool
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_agent_runner.py:4309)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_agent_runner_streamed.py:1618)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_agent_runner.py:4309)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_agent_runner_streamed.py:1618)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  alpha_tool
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_run_state.py:362)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_run_state.py:402)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_run_state.py:362)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_run_state.py:402)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  already_ran
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_hitl_error_scenarios.py:1722)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_hitl_error_scenarios.py:1765)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_hitl_error_scenarios.py:1722)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_hitl_error_scenarios.py:1765)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  bad_tool
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_hitl_error_scenarios.py:872)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_hitl_error_scenarios.py:887)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_hitl_error_scenarios.py:872)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_hitl_error_scenarios.py:887)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  beta_tool
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_run_state.py:366)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_run_state.py:406)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_run_state.py:366)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_run_state.py:406)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  deferred_lookup
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_function_tool.py:505)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_function_tool_decorator.py:153)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_function_tool.py:505)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_function_tool_decorator.py:153)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  fast_tool
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_guardrails.py:482)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_guardrails.py:624)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_guardrails.py:482)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_guardrails.py:624)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  inner_hitl_tool
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_hitl_error_scenarios.py:296)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_hitl_error_scenarios.py:358)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_hitl_error_scenarios.py:296)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_hitl_error_scenarios.py:358)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  lookup_account
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_hitl_error_scenarios.py:1187)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_tool_origin.py:76)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_hitl_error_scenarios.py:1187)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_tool_origin.py:76)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  pending_me
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_hitl_error_scenarios.py:1491)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_hitl_error_scenarios.py:2067)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_hitl_error_scenarios.py:1491)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_hitl_error_scenarios.py:2067)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  tool2
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_agent_runner.py:4304)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_agent_runner_streamed.py:1613)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_agent_runner.py:4304)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_agent_runner_streamed.py:1613)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  all_optional_params_function
    [OAI-003]  MED Tool sets strict_mode=False  (tests/test_function_tool_decorator.py:186)
        Setting strict_mode=False on @function_tool relaxes JSON-schema enforcement at             
        runtime. The model can then pass arguments that do not match the tool's type hints,
        leading to silent type errors or crashes inside the tool body.                     
        fix: Remove the strict_mode=False kwarg (the default True is the safe setting). If the relaxation was needed for a specific input shape, adjust the type hints to accept that shape explicitly.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_function_tool_decorator.py:186)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_function_tool_decorator.py:186)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  optional_param_function
    [OAI-003]  MED Tool sets strict_mode=False  (tests/test_function_tool_decorator.py:162)
        Setting strict_mode=False on @function_tool relaxes JSON-schema enforcement at             
        runtime. The model can then pass arguments that do not match the tool's type hints,
        leading to silent type errors or crashes inside the tool body.                     
        fix: Remove the strict_mode=False kwarg (the default True is the safe setting). If the relaxation was needed for a specific input shape, adjust the type hints to accept that shape explicitly.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_function_tool_decorator.py:162)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_function_tool_decorator.py:162)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  get_current_weather
    [MCP-004]  LOW MCP tool has no description  (examples/mcp/sse_example/server.py:27)
        MCP tools expose a description field to the orchestrating model so it knows when to        
        call the tool. Without a docstring, the server omits the description, and the model
        must guess from the function name alone — which causes mis-selection or skipped    
        calls under ambiguous prompts.                                                     
        fix: Add a one-paragraph docstring describing what the tool does, its inputs, and its outputs. The MCP framework surfaces this as the tool description to any connected client.

    [MCP-004]  LOW MCP tool has no description  (examples/mcp/streamablehttp_example/server.py:28)
        MCP tools expose a description field to the orchestrating model so it knows when to        
        call the tool. Without a docstring, the server omits the description, and the model
        must guess from the function name alone — which causes mis-selection or skipped    
        calls under ambiguous prompts.                                                     
        fix: Add a one-paragraph docstring describing what the tool does, its inputs, and its outputs. The MCP framework surfaces this as the tool description to any connected client.


  approval_echo
    [OAI-004]  MED Tool has no failure_error_function  (examples/memory/hitl_session_scenario.py:46)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_hitl_session_scenario.py:42)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_hitl_session_scenario.py:42)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  approval_note
    [OAI-004]  MED Tool has no failure_error_function  (examples/memory/hitl_session_scenario.py:56)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_hitl_session_scenario.py:52)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_hitl_session_scenario.py:52)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  get_discount_approval_rule
    [OAI-004]  MED Tool has no failure_error_function  (examples/sandbox/sandbox_agents_as_tools.py:73)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_example_workflows.py:588)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_example_workflows.py:588)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  get_current_time
    [OAI-004]  MED Tool has no failure_error_function  (examples/realtime/twilio/twilio_handler.py:30)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (tests/test_streaming_tool_call_arguments.py:330)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.


  get_temperature
    [OAI-004]  MED Tool has no failure_error_function  (examples/agent_patterns/human_in_the_loop.py:40)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (examples/agent_patterns/human_in_the_loop_stream.py:26)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.


  multiply_by_two
    [OAI-004]  MED Tool has no failure_error_function  (examples/basic/agent_lifecycle_example.py:83)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (examples/basic/lifecycle_example.py:122)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.


  random_number
    [OAI-004]  MED Tool has no failure_error_function  (examples/basic/agent_lifecycle_example.py:66)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (examples/basic/lifecycle_example.py:116)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.


  random_number_tool
    [OAI-004]  MED Tool has no failure_error_function  (examples/handoffs/message_filter.py:12)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAI-004]  MED Tool has no failure_error_function  (examples/handoffs/message_filter_streaming.py:12)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.


  create_config
    [OAI-004]  MED Tool has no failure_error_function  (examples/basic/stream_function_call_args.py:16)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-006]  MED Mutating OpenAI tool has no idempotency key  (examples/basic/stream_function_call_args.py:16)
        Tool name suggests a side effect (create/send/refund/…). The OpenAI Agents SDK              
        retries tool calls on timeout and ambiguous failures; without an idempotency key the
        same action can fire twice. Downstream services must also honor the key for this    
        protection to be effective.                                                         
        fix: Add an `idempotency_key: str` parameter and pass it to the backing API.


  send_email
    [OAI-004]  MED Tool has no failure_error_function  (examples/basic/tool_guardrails.py:18)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-006]  MED Mutating OpenAI tool has no idempotency key  (examples/basic/tool_guardrails.py:18)
        Tool name suggests a side effect (create/send/refund/…). The OpenAI Agents SDK              
        retries tool calls on timeout and ambiguous failures; without an idempotency key the
        same action can fire twice. Downstream services must also honor the key for this    
        protection to be effective.                                                         
        fix: Add an `idempotency_key: str` parameter and pass it to the backing API.


  update_customer_record
    [OAI-004]  MED Tool has no failure_error_function  (examples/realtime/twilio_sip/agents.py:36)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-006]  MED Mutating OpenAI tool has no idempotency key  (examples/realtime/twilio_sip/agents.py:36)
        Tool name suggests a side effect (create/send/refund/…). The OpenAI Agents SDK              
        retries tool calls on timeout and ambiguous failures; without an idempotency key the
        same action can fire twice. Downstream services must also honor the key for this    
        protection to be effective.                                                         
        fix: Add an `idempotency_key: str` parameter and pass it to the backing API.


  get_secret_word
    [MCP-004]  LOW MCP tool has no description  (examples/mcp/sse_example/server.py:21)
        MCP tools expose a description field to the orchestrating model so it knows when to        
        call the tool. Without a docstring, the server omits the description, and the model
        must guess from the function name alone — which causes mis-selection or skipped    
        calls under ambiguous prompts.                                                     
        fix: Add a one-paragraph docstring describing what the tool does, its inputs, and its outputs. The MCP framework surfaces this as the tool description to any connected client.

    [MCP-004]  LOW MCP tool has no description  (examples/mcp/streamablehttp_custom_client_example/server.py:21)
        MCP tools expose a description field to the orchestrating model so it knows when to        
        call the tool. Without a docstring, the server omits the description, and the model
        must guess from the function name alone — which causes mis-selection or skipped    
        calls under ambiguous prompts.                                                     
        fix: Add a one-paragraph docstring describing what the tool does, its inputs, and its outputs. The MCP framework surfaces this as the tool description to any connected client.

    [MCP-004]  LOW MCP tool has no description  (examples/mcp/streamablehttp_example/server.py:22)
        MCP tools expose a description field to the orchestrating model so it knows when to        
        call the tool. Without a docstring, the server omits the description, and the model
        must guess from the function name alone — which causes mis-selection or skipped    
        calls under ambiguous prompts.                                                     
        fix: Add a one-paragraph docstring describing what the tool does, its inputs, and its outputs. The MCP framework surfaces this as the tool description to any connected client.


  another_tool
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_function_tool.py:472)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_function_tool.py:472)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  async_no_context
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_function_tool_decorator.py:64)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_function_tool_decorator.py:64)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  async_with_context
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_function_tool_decorator.py:78)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_function_tool_decorator.py:78)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  deferred_lookup_account
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_hitl_error_scenarios.py:1064)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_hitl_error_scenarios.py:1064)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  delegate_tool
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_agent_runner.py:1120)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_agent_runner.py:1120)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  disabled_tool
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_function_tool.py:465)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_function_tool.py:465)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  echo_tool
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_agent_runner.py:4342)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_agent_runner.py:4342)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  first_approval_tool
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_hitl_error_scenarios.py:1127)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_hitl_error_scenarios.py:1127)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  foo_tool
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_agent_tracing.py:76)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_agent_tracing.py:76)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  get_current_timestamp
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_hitl_error_scenarios.py:932)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_hitl_error_scenarios.py:932)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  get_user_name
    [OAI-004]  MED Tool has no failure_error_function  (examples/agent_patterns/agents_as_tools_conditional.py:31)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (examples/agent_patterns/agents_as_tools_conditional.py:31)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  greet
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_agent_clone_shallow_copy.py:5)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_agent_clone_shallow_copy.py:5)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  guarded
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_function_tool.py:824)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_function_tool.py:824)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  inner_sensitive_tool
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_run_state.py:2705)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_run_state.py:2705)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  inner_shared_tool
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_hitl_error_scenarios.py:408)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_hitl_error_scenarios.py:408)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  inner_tool
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_hitl_error_scenarios.py:1672)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_hitl_error_scenarios.py:1672)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  lookup_customer_profile
    [OAI-004]  MED Tool has no failure_error_function  (examples/memory/file_hitl_example.py:129)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (examples/memory/file_hitl_example.py:129)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  lookup_secret
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_run_step_execution.py:1500)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_run_step_execution.py:1500)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  other_tool
    [OAI-004]  MED Tool has no failure_error_function  (tests/extensions/experiemental/codex/test_codex_tool.py:1209)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/extensions/experiemental/codex/test_codex_tool.py:1209)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  outer_shared_tool
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_hitl_error_scenarios.py:404)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_hitl_error_scenarios.py:404)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  outer_tool
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_hitl_error_scenarios.py:1668)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_hitl_error_scenarios.py:1668)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  pending_tool
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_hitl_error_scenarios.py:1878)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_hitl_error_scenarios.py:1878)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  record_side_effect
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_run_step_execution.py:1506)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_run_step_execution.py:1506)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  second_approval_tool
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_hitl_error_scenarios.py:1132)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_hitl_error_scenarios.py:1132)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  sync_no_context_no_args
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_function_tool_decorator.py:26)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_function_tool_decorator.py:26)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  sync_no_context_override
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_function_tool_decorator.py:92)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_function_tool_decorator.py:92)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  sync_no_context_with_args
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_function_tool_decorator.py:38)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_function_tool_decorator.py:38)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  sync_tool_decorator_style
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_function_tool.py:978)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_function_tool.py:978)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  sync_with_context
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_function_tool_decorator.py:51)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_function_tool_decorator.py:51)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  synthetic_tool
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_run_step_execution.py:1532)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_run_step_execution.py:1532)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  test_tool_one
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_agent_runner.py:3247)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_agent_runner.py:3247)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  test_tool_two
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_agent_runner.py:3252)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_agent_runner.py:3252)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  timeout_configured_tool
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_function_tool_decorator.py:256)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_function_tool_decorator.py:256)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  tool_one
    [OAI-004]  MED Tool has no failure_error_function  (tests/realtime/test_runner.py:208)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/realtime/test_runner.py:208)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  triage_tool
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_agent_runner.py:1116)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_agent_runner.py:1116)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  visible_lookup_account
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_hitl_error_scenarios.py:1056)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.

    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_hitl_error_scenarios.py:1056)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  timeout_tool
    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_function_tool.py:890)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.

    [OAIS-005]  MED OpenAI tool raises exceptions without a structured error contract  (tests/test_function_tool.py:890)
        When a @function_tool raises, the SDK surfaces the exception to the model as an opaque       
        string. The model often cannot recover or retry intelligently. Returning a structured
        error object lets the model branch on the failure mode and decide whether to retry or
        escalate.                                                                            
        fix: Catch known failure modes and return a structured payload such as `{"error": "...", "retryable": False}` instead of raising.


  billing_status_checker
    [OAI-004]  MED Tool has no failure_error_function  (examples/agent_patterns/agents_as_tools_streaming.py:10)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.


  calculate_sum
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_streaming_tool_call_arguments.py:134)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.


  fetch_random_image
    [OAI-004]  MED Tool has no failure_error_function  (examples/basic/image_tool_output.py:11)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.


  format_message
    [OAI-004]  MED Tool has no failure_error_function  (tests/test_streaming_tool_call_arguments.py:140)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.


  get_contact_info
    [OAI-004]  MED Tool has no failure_error_function  (examples/basic/tool_guardrails.py:37)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.


  get_customer_profile
    [OAI-004]  MED Tool has no failure_error_function  (examples/tools/tool_search.py:46)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.


  get_discount_approval_path
    [OAI-004]  MED Tool has no failure_error_function  (examples/sandbox/sandbox_agent_with_tools.py:36)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.


  get_invoice_status
    [OAI-004]  MED Tool has no failure_error_function  (examples/tools/tool_search.py:62)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.


  get_shipping_credit_balance
    [OAI-004]  MED Tool has no failure_error_function  (examples/tools/tool_search.py:78)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.


  get_shipping_eta
    [OAI-004]  MED Tool has no failure_error_function  (examples/tools/tool_search.py:70)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.


  get_user_data
    [OAI-004]  MED Tool has no failure_error_function  (examples/basic/tool_guardrails.py:24)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.


  helper
    [OAI-004]  MED Tool has no failure_error_function  (tests/realtime/test_realtime_model_settings.py:48)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.


  how_many_jokes
    [OAI-004]  MED Tool has no failure_error_function  (examples/basic/stream_items.py:8)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.


  list_open_orders
    [OAI-004]  MED Tool has no failure_error_function  (examples/tools/tool_search.py:54)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.


  lookup_insurance_eligibility
    [OAI-004]  MED Tool has no failure_error_function  (examples/sandbox/healthcare_support/tools.py:51)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.


  lookup_order
    [OAI-004]  MED Tool has no failure_error_function  (examples/basic/stream_ws.py:39)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.


  lookup_patient
    [OAI-004]  MED Tool has no failure_error_function  (examples/sandbox/healthcare_support/tools.py:36)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.


  lookup_referral_status
    [OAI-004]  MED Tool has no failure_error_function  (examples/sandbox/healthcare_support/tools.py:66)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.


  ping
    [OAI-004]  MED Tool has no failure_error_function  (tests/realtime/test_realtime_model_settings.py:83)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.


  publish_announcement
    [OAI-004]  MED Tool has no failure_error_function  (examples/agent_patterns/human_in_the_loop_custom_rejection.py:33)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.


  query_runloop_network_policy
    [OAI-004]  MED Tool has no failure_error_function  (examples/sandbox/extensions/runloop/capabilities.py:518)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.


  query_runloop_secret
    [OAI-004]  MED Tool has no failure_error_function  (examples/sandbox/extensions/runloop/capabilities.py:510)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.


  route_to_human_queue
    [OAI-004]  MED Tool has no failure_error_function  (examples/sandbox/healthcare_support/tools.py:87)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.


  submit_refund
    [OAI-004]  MED Tool has no failure_error_function  (examples/basic/stream_ws.py:73)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.


  test_tool
    [OAI-004]  MED Tool has no failure_error_function  (tests/extensions/memory/test_advanced_sqlite_session.py:27)
        When a tool raises an exception, the OpenAI Agents SDK surfaces the exception's string     
        representation to the model by default. The model receives an opaque error with no 
        recovery contract, leading to hallucinated retries or confused responses. Setting  
        failure_error_function lets you control what the model sees, typically a structured
        JSON describing the failure and what input to try instead.                         
        fix: Pass failure_error_function= to the decorator. The function should receive the exception and the run context, and return a structured string the model can reason about.


  boom
    [OAIS-005]  MED OpenAI tool raises exceptions without a structured error contract  (tests/test_function_tool.py:566)
        When a @function_tool raises, the SDK surfaces the exception to the model as an opaque       
        string. The model often cannot recover or retry intelligently. Returning a structured
        error object lets the model branch on the failure mode and decide whether to retry or
        escalate.                                                                            
        fix: Catch known failure modes and return a structured payload such as `{"error": "...", "retryable": False}` instead of raising.


  add
    [MCP-004]  LOW MCP tool has no description  (examples/mcp/manager_example/mcp_server.py:16)
        MCP tools expose a description field to the orchestrating model so it knows when to        
        call the tool. Without a docstring, the server omits the description, and the model
        must guess from the function name alone — which causes mis-selection or skipped    
        calls under ambiguous prompts.                                                     
        fix: Add a one-paragraph docstring describing what the tool does, its inputs, and its outputs. The MCP framework surfaces this as the tool description to any connected client.


  apply_lifecycle_patch
    [OAIS-001]  LOW OpenAI tool has no description  (tests/sandbox/integration_tests/_helpers.py:376)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  assert_manifest_materialized_tool
    [OAIS-001]  LOW OpenAI tool has no description  (tests/sandbox/integration_tests/_helpers.py:370)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  assert_restored_lifecycle_state_tool
    [OAIS-001]  LOW OpenAI tool has no description  (tests/sandbox/integration_tests/_helpers.py:423)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  assert_workspace_escape_blocked_tool
    [OAIS-001]  LOW OpenAI tool has no description  (tests/sandbox/integration_tests/_helpers.py:384)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  echo
    [MCP-004]  LOW MCP tool has no description  (examples/mcp/manager_example/mcp_server.py:21)
        MCP tools expose a description field to the orchestrating model so it knows when to        
        call the tool. Without a docstring, the server omits the description, and the model
        must guess from the function name alone — which causes mis-selection or skipped    
        calls under ambiguous prompts.                                                     
        fix: Add a one-paragraph docstring describing what the tool does, its inputs, and its outputs. The MCP framework surfaces this as the tool description to any connected client.


  extract_lifecycle_archive
    [OAIS-001]  LOW OpenAI tool has no description  (tests/sandbox/integration_tests/_helpers.py:390)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  start_lifecycle_pty
    [OAIS-001]  LOW OpenAI tool has no description  (tests/sandbox/integration_tests/_helpers.py:397)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  will_fail_on_bad_json
    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_function_tool_decorator.py:106)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  will_not_fail_on_bad_json
    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_function_tool_decorator.py:124)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


  will_not_fail_on_bad_json_async
    [OAIS-001]  LOW OpenAI tool has no description  (tests/test_function_tool_decorator.py:141)
        The OpenAI Agents SDK surfaces the @function_tool's docstring as the tool description      
        shown to the model. With no description the model must guess from the function name
        when to call this tool — which causes mis-selection under ambiguous prompts.       
        fix: Add a one-paragraph docstring describing inputs, outputs, and when to use this tool.


Generated artifacts
  hooks/pretooluse_validate.py  (claude_sdk)
    Generated from 407 hook-eligible findings across 6 tools. Re-runs are byte-stable.
  hooks/posttooluse_log.py  (claude_sdk)
    Structured logging hook. Phase 2 will route these events to OTEL.
  openshell/policy.yaml  (openshell)
    Generated from 21 OpenShell finding(s).
